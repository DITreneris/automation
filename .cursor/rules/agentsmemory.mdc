---
description: Local palace wake — Use checkout (promptanatomy.info)
alwaysApply: true
---

# agentsmemory — .info checkout

This folder is the Prompt Anatomy **Use** library (`promptanatomy.info`, repo `DITreneris/automation`). Memory is the local palace at `http://127.0.0.1:8080/mcp` (user-level MCP). Cursor has no Stop hook — this rule is the write half.

Default wing: `promptanatomy`. **Do not mint an `info` or `automation` wing.** MCP `default_wing` is empty — every `am_*` call must pass `wing: promptanatomy`. `wing: "*"` only for a deliberate cross-project question.

This is not the Hub and not Enter. Enter is `DITreneris/lead` (`promptanatomy.cloud`).

## First actions of every session

1. `am_status` — `mode` must be `local`, workspace slug `local`. A different workspace is a stop — write nothing.
2. `am_skillset` and follow it.
3. `am_load_skill("promptanatomy")`.
4. `am_bootstrap` with `wing: promptanatomy`.

## Recall

Use-spoke role, 30-day first-improve (share DONE 2026-09-01 — do not re-ask or chase bounce), four `.app` URLs (`COURSE_URL_EN` badge, `COURSE_COMMUNITY_URL`, `COURSE_RITUAL_URL`, `HUB_ENTITY_URL`), two-channel pricing (39/99 vs 399) → `am_search` wing `promptanatomy`. Checkout and access live on `.app`, not here.

## Persist

`am_diary_write` agent_name `cursor-memory`, wing `promptanatomy` only when this session settled a **new** ecosystem fact. Do not re-file hub deploy/access canon.

## Never mine into the palace

This spoke's source, course JSON, sibling spoke repos. Do not dump the library tree into drawers. Code stays here; palace stays memory.
